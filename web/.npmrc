# The Privy /solana subpath pins @solana-program/memo@0.10 against a newer
# @solana/kit, which npm flags as a peer-dep conflict. We install with legacy
# peer resolution locally; this makes Vercel's `npm install` do the same so the
# build doesn't fail on the conflict.
legacy-peer-deps=true
